 // Date of birth in YYYY-MM-DD format
let dob = "2000-05-15";

// Convert DOB string into a Date object
let date1 = new Date(dob);

// Get the current date
let date2 = new Date();

// Calculate age by subtracting birth year from current year
// (This gives an approximate age, not considering month and day)
console.log(date2.getFullYear() - date1.getFullYear());
// Date of birth in YYYY-MM-DD format
let dob = "2000-05-15";

// Convert DOB string into a Date object
let date1 = new Date(dob);

// Get the current date
let date2 = new Date();

// Calculate age by subtracting birth year from current year
// (This gives an approximate age, not considering month and day)
console.log(date2.getFullYear() - date1.getFullYear());
